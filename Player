<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Player Audio Minimale</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f0f0f0;
    }

    .player-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      width: 100%;
      max-width: 400px;
    }

    .cover-container {
      width: 100%;
      max-width: 300px;
      margin-bottom: 20px;
    }

    .cover {
      width: 100%;
      border-radius: 10px;
      object-fit: cover;
    }

    audio {
      width: 100%;
      outline: none;
    }
  </style>
</head>
<body>
  <div class="player-container">
    <div class="cover-container">
      <img id="cover" src="default-cover.jpg" alt="Copertina" class="cover">
    </div>
    <audio id="audio-player" controls>
      <source src="https://s3.yesstreaming.net:19000" type="audio/mpeg">
      Il tuo browser non supporta l'elemento audio.
    </audio>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", function() {
      const coverImg = document.getElementById("cover");
      const audioPlayer = document.getElementById("audio-player");

      // Puoi aggiungere un'API per recuperare la copertina dal flusso audio
      // Qui simulerò semplicemente il caricamento di una nuova immagine dopo un certo intervallo.
      setTimeout(() => {
        coverImg.src = "new-cover.jpg"; // Sostituisci con la URL della copertina dinamica
      }, 3000); // Simulazione del caricamento della copertina
    });
  </script>
</body>
</html>
